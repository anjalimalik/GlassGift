describe("Donations", () => {
    it("A donation outside a minimum/maximum limit of an NGO should fail", () => {

    });

    it("Viewing anonymous payments shouldn't reveal the donor's identity", () => {

    });
});